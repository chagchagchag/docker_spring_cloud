server:
  port: 7776
spring:
  application:
    name: docker-admin-dashboard
  config:
    import:
      - classpath:application-compose.yml
      - classpath:local-compose-eureka.yml
      - classpath:actuator.yml
      - classpath:spring-boot-admin-compose.yml
logging:
  level:
    root: debug
